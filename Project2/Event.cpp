#include "Event.h"

event::event(int zmienna)
{
	
}

/*void event::book(player)

	{
		cout << "Znajdujesz tajemnicza ksiege, im blizej do niej podchodzisz tym wieksza groze odczuwasz. Czy chcesz otworzyc ksiege? (1.Tak / 2.Nie)" << endl;
		int option;
		int error = 1;
		cin >> option;
		while (error)
		{
			switch (option)
			{
			case 1:
				int chance;
				chance = rand() % 1000 + 1;
				if ((getMaxMana() * 5) > chance)
				{
					setMaxMana(getMaxMana() + getMaxMana() / 2);
					setMana(getMaxMana());
					cout << "Ksiega jest niesamowitym zrodlem wiedzy magicznej" << endl;
				}
				else
				{
					setHealth(1);
					setStamina(1);
					setMana(1);
					cout << "Ksiega przerasta twoje umiejetnosci pozostawiajac cie w oplakanym stanie" << endl;
				}
				error = 0;
				break;
			case 2:
				cout << "Wolisz nie ryzykowac. Odchodzisz od ksiegi." << endl;
				error = 0;
				break;
			default:
				cout << "Wybierz poprawna opcje." << endl;
				break;
			}

		}
	}*/

